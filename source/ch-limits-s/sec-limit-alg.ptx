<?xml version="1.0" encoding="UTF-8"?>
<section xmlns:xi="http://www.w3.org/2001/XInclude" xml:lang="en-US" xml:id="sec-limit-laws">
  <title>Limit Laws and Algebraic Techniques</title>

  <objectives>
    <ul>
      <li>State and apply the basic <term>Limit Laws</term> (sum, difference, constant multiple, product, quotient, power, root).</li>
      <li>Recognize when direct substitution can be used to evaluate a limit and when it leads to meaningless expressions.</li>
      <li>Evaluate limits that initially give meaningless expressions by algebraic manipulation </li>
      <li>Analyze limits of piecewise-defined functions by considering one-sided limits.</li>
      <li>Apply the <term>Squeeze Theorem</term> to evaluate limits of functions bounded above and below.</li>
    </ul>
  </objectives>

  <subsection xml:id="ssec-limit-laws">
    <title>Operations on Limits</title>

    <p>
      Some general combination rules make most limit computations routine. 
    </p>

<theorem xml:id="thm-limit-laws">
<title>Limit Laws</title>
<statement>
<p>
Suppose we know that both <m>\displaystyle \lim_{x\to a} f(x)</m> and <m>\displaystyle \lim_{x\to a} g(x)</m> exist. Then we have the following Limit Laws:
</p>
<ul>
<li><em>Sum:</em> <m>\displaystyle \lim_{x\to a} (f(x) + g(x)) = \lim_{x\to a} f(x) + \lim_{x\to a} g(x)</m>.</li>
<li><em>Difference:</em> <m>\displaystyle \lim_{x\to a} (f(x) - g(x)) = \lim_{x\to a} f(x) - \lim_{x\to a} g(x)</m>.</li>
<li><em>Constant Multiple:</em> <m>\displaystyle \lim_{x\to a} (c f(x)) = c \cdot \lim_{x\to a} f(x)</m>, for a constant <m>c</m>.</li>
<li><em>Product:</em> <m>\displaystyle \lim_{x\to a} f(x) g(x) = (\lim_{x\to a} f(x))(\lim_{x\to a} g(x))</m>.</li>
<li><em>Quotient:</em> <m>\displaystyle \lim_{x\to a} \frac{f(x)}{g(x)} = \frac{\lim_{x\to a} f(x)}{\lim_{x\to a} g(x)}</m>, provided <m>\lim_{x\to a} g(x) \
eq 0</m>.</li>
<li><em>Power:</em> <m>\displaystyle \lim_{x\to a} (f(x))^n = (\lim_{x\to a} f(x))^n</m>, for a whole number <m>n</m>.</li>
<li><em>Root:</em> <m>\displaystyle \lim_{x\to a} \sqrt[n]{f(x)} = \sqrt[n]{\lim_{x\to a} f(x)}</m>, for a whole number<fn>If <m>n</m> is even, we assume <m>\displaystyle\lim_{x\to a} f(x) &gt; 0</m></fn> <m>n</m>.</li>
</ul>
<p>
These all have the form: “The limit of an operation equals the operation applied to the limits.” These Laws are also valid for one-sided limits.
</p>
</statement>
<proof>
<p>
A full proof of these laws requires high-level mathematics and the formal <m>\varepsilon</m>–<m>\delta</m> definition of limits (beyond the scope of this course). 
Rigorous proofs like this can be found in MSU's MTH 320 Real Analysis course. 
For those who would like a glimpse a Real Analysis proof, see <url href="https://www.youtube.com/watch?v=eZ13wOvSg64">this video proving the Limit Sum Law</url> 

</p>
</proof>
</theorem>


<problem xml:id="prob-limit-laws-simple">
<statement>
<p>
Suppose <m>\displaystyle\lim_{x\to 5} f(x) = 2</m> and <m>\displaystyle\lim_{x\to 5} g(x) = 3</m>. Find <m>\displaystyle\lim_{x\to 5} \left(2f(x) - (g(x))^2\right)</m>.
</p>
</statement>
<solution>
<p>
Using the Difference, Constant Multiple, and Power Limit Laws:
</p>
<me>
2\cdot \lim_{x\to 5} f(x) - \left(\lim_{x\to 5} g(x)\right)^2 = 2(2) - (3)^2 = 4 - 9 = -5.
</me>
</solution>
</problem>

  </subsection>

  <subsection xml:id="ssec-plugging-in">
    <title>Limits by Plugging In</title>

    <p>
      Assuming the Limit Laws and the Basic Limits <m>\displaystyle\lim_{x\to a} x = a</m> and <m>\displaystyle\lim_{x\to a} c = c</m>, we can prove that most functions are continuous, meaning the limit can be obtained by direct substitution.
    </p>

    <example xml:id="ex-limit-plug-in">
      <title>A Limit by Substitution</title>
      <statement>
        <p>
          Compute <m>\displaystyle \lim_{x\to 2} \frac{1-\sqrt{x}}{1+x}</m> using the Limit Laws.
        </p>
      </statement>
      <solution>
        <p><alert>Step by Step:</alert></p>
        <me>
          \begin{array}{rcll}
           \displaystyle\lim_{x\to 2} \frac{1-\sqrt{x}}{1+x}
          \amp=\amp 
            \displaystyle\frac{\displaystyle\lim_{x\to 2}\,1{-}\sqrt{x}}{\displaystyle\lim_{x\to 2}\,1+x}
          \amp \text{ by the Quotient Law
          }
          \\[2em]
          \amp=\amp 
            \displaystyle\frac{\displaystyle\lim_{x\to 2}\,1-\displaystyle\lim_{x\to 2}\sqrt{x}}{\displaystyle\lim_{x\to 2}1\,+\,\displaystyle\lim_{x\to 2}x}
          \amp \text{ by the Sum and Difference Laws}\!\!\!
          \\[2em]
          \amp=\amp 
            \displaystyle\frac{\displaystyle\lim_{x\to 2}\,1-\sqrt{\displaystyle\lim_{x\to 2}x}}{\displaystyle\lim_{x\to 2}1\,+\,\displaystyle\lim_{x\to 2}x}
          \amp \text{ by the Root Law}
          \\[2em]
          \amp=\amp 
            \displaystyle\frac{1{-}\sqrt{2}}{1\,{+}\,2}
          \ =\ \frac{1{-}\sqrt{2}}{3}
          \amp \text{ by the Basic Limits.}
          \end{array}
        </me>
        <p>
          Thus the correct limit is obtained simply by substituting <m>x=2</m>.
        </p>
        <p>
          <alert>Note:</alert> The Quotient Law requires that the denominator have a non-zero limit. 
          We tentatively proceed with the computation and find the denominator to be 3, 
          which retrospectively justifies the quotient step.
        </p>
      </solution>
    </example>

    <remark>
      <p>
        Substituting <m>x=a</m> usually gives the correct limit unless it leads to a meaningless expression such as <m>\tfrac{0}{0}</m> or <m>\sqrt{-1}</m> (complex values are not considered in this course).
      </p>
    </remark>

    <p>
      In <xref ref="sec-derivative-trig">Section</xref> and <xref ref="sec-derivative-expChain">Section</xref>, we will verify that trigonometric functions 
 like <m>\sin(x)</m> and <m>\tan(x)</m> are also continuous when defined, 
and the same holds for functions like <m>2^x</m> and <m>\ln(x)</m>. 
Thus, this principle works for pretty much all reasonable functions.
    </p>

  </subsection>

  <subsection xml:id="ssec-canceling-zeros">
    <title>Limits by Canceling Zeros</title>

    <p>
      Some of the most important limits are those where substitution gives a meaningless expression like <m>\tfrac{0}{0}</m>. To compute these, we must cancel vanishing factors until the expression can be evaluated.
      This often requires algebraic manipulation such as factoring or multiplying by a conjugate.
    </p>

    <example xml:id="ex-canceling-zeros">
      <title>Canceling Factors</title>
      <statement>
        <p>
          Evaluate <m>\displaystyle \lim_{x\to 2} \frac{x^2-4x+4}{x^2-x-2}</m>.
        </p>
      </statement>
      <solution>
        <p>
          Direct substitution gives the meaningless expression <m>\tfrac{0}{0}</m>, so we must simplify the expression first. Factor numerator and denominator:
        </p>
        <me>
          \begin{array}{rcl}
           \displaystyle\lim_{x\to 2}\frac{(x-2)^2}{(x-2)(x+1)} \amp=\amp \displaystyle\lim_{x\to 2} \frac{x-2}{x+1} \\
          \amp=\amp \frac{0}{3} = 0.
          \end{array}
        </me>

          <remark>
        <p> 
Please note that this example is consistent with what we saw in the previous section. 
Here is a Desmos graph of the function, along with a table of values near <m>x=2</m>. </p>

<url href="https://www.desmos.com/calculator/swij6khoz5"> 
<sidebyside width = "90%">
<figure>
  <caption>
    Graph of <m>f(x)=\dfrac{x^2-4x+4}{x^2-x-2}</m>
  </caption>
  <image width = "100%" source="images/LimitOfFunction001.gif">
  </image>
</figure>
</sidebyside>
</url>

<p> 
Keep in mind, however, that on quizzes and exams you may not have access to a calculator — in those cases the algebraic method will be our primary tool for solving such problems.

 </p>
 </remark>
      </solution>
    </example>

    <example xml:id="ex-conjugate">
      <title>Using a Conjugate</title>
      <statement>
        <p>
          Evaluate <m>\displaystyle \lim_{x\to 9} \frac{x-9}{\sqrt{x}-3}</m>.
        </p>
      </statement>
      <solution>
        <p>
          Direct substitution gives the meaningless expression <m>\tfrac{0}{0}</m>, so we must simplify the expression first. 
        </p>
<p>
  Multiply numerator and denominator by the conjugate <m>\sqrt{x}+3</m>:
</p>
<me>
  \displaystyle\lim_{x\to 9}\frac{x-9}{\sqrt{x}-3}\cdot\frac{\sqrt{x}+3}{\sqrt{x}+3}
  = \lim_{x\to 9}\frac{(x-9)(\sqrt{x}+3)}{(\sqrt{x})^2-3^2}
</me>

<p>
  Simplify the denominator using the difference of squares:
</p>
<me>
  \displaystyle\lim_{x\to 9}\frac{(x-9)(\sqrt{x}+3)}{x-9}
</me>>

<p>
  Now cancel the common factor <m>x-9</m>:
</p>
<me>
  \displaystyle\lim_{x\to 9} (\sqrt{x}+3) = 6.
</me>
  <remark>
<p>
  Again, this algebraic approach is important, but we can also explore the behavior graphically or with tables. 
  View it here: <url href="https://www.desmos.com/calculator/ldglp9hhbw" visual="">Desmos Graph</url>.
</p>
</remark>
      </solution>
    </example>
  </subsection>

  <subsection xml:id="ssec-cases">
    <title>Limits by Cases</title>

    <p>
      As we learned in <xref ref="sec-graphical-limits">Section</xref>, piecewise functions often require evaluating limits separately from the left and right. 
      One key example of this is the absolute value function <m>|x|</m>, which by <xref ref="def-absolute-value">its definition</xref> is really a piecewise function. 
      Therefore limits involving absolute values often require a case-by-case analysis.
    </p>

    <example xml:id="ex-abs-value">
      <title>Absolute Value Example</title>
      <statement>
        <p>
          Evaluate <m>\displaystyle \lim_{x\to 2} \frac{|x^2-4|}{x-2}</m>.
        </p>
      </statement>
      <solution>
        <p>
          <alert>Check Your Answers:</alert>
        </p>
        <p> 
          <m>\displaystyle \lim_{x\to 2} \frac{|x^2-4|}{x-2}</m> does not exist, because the left-hand and right-hand limits are different.
        </p>
          <p><alert>Step by Step Video Solution:</alert></p>
          <p>
              <video youtube="eYcCeFBrLqc" width="90%">
              </video>
          </p>
          <p>
  Again, this algebraic approach is important, but we can also explore the behavior graphically or with tables. 
  View it here: <url href="https://www.desmos.com/calculator/stajfp6rnn" visual="">Desmos Graph</url>.
</p>


    </solution>
    </example>
  </subsection>

  <subsection xml:id="ssec-squeeze">
    <title>Limits by Squeezing</title>


<p>
  Some limits <m>\displaystyle\lim_{x\to a} f(x)</m> are difficult to evaluate because the function
  <m>f(x)</m> behaves erratically near <m>x=a</m>. For example:
</p>

<me>
  \lim_{x\to 1} \Bigl(x + 4(x-1)^2 \sin\!\bigl(\tfrac{1}{x-1}\bigr)\Bigr).
</me>

<p>
  The graph shows the unusual behavior, oscillating faster and faster near <m>x=1</m>
  because <m>\sin(\theta)</m> completes infinitely many periods as
  <m>\theta = \tfrac{1}{x-1}</m> becomes larger.
</p>



<sidebyside width = "90%">
<figure xml:id="fig-squeeze-oscillation">

  <caption>
     The function <m>y = x + 4(x-1)^2\sin\!\bigl(1/(x-1)\bigr)</m> (solid) squeezed between
    <m>y = x \pm 4(x-1)^2</m> (dashed).
  </caption>
  <image width = "100%" source="images/0102-g1.PNG">
  </image>
</figure>
</sidebyside>


<p>
  However, we can still find the limit using the <term>Squeeze Theorem</term>, which applies when
  <m>f(x)</m> is squeezed between two other functions. </p> 



    <theorem xml:id="thm-squeeze">
      <title>Squeeze Theorem</title>
      <statement>
        <p>
          Suppose <m>g(x) \leq f(x) \leq h(x)</m> for all <m>x</m> near <m>a</m> (except possibly <m>x=a</m>), and <m>\displaystyle\lim_{x\to a} g(x) = \displaystyle\lim_{x\to a} h(x) = L</m>. Then <m>\displaystyle\lim_{x\to a} f(x) = L</m>.
        </p>
      </statement>
    </theorem>
 

  <p> 
    Now let's apply the Squeeze Theorem to our example
  </p>

    <example xml:id="ex-squeeze-1">
      <title>Squeezing Example</title>
      <statement>
        <p>
          Evaluate <m>\displaystyle \lim_{x\to 1} x + 4(x-1)^2\sin(\tfrac{1}{x-1})</m>.
        </p>
      </statement>
<solution>
  <p>
    First note the expression is defined for <m>x \neq 1</m>, which is fine for a limit:
    we only need values of <m>x</m> near (but not equal to) <m>1</m>.
  </p>

  <p>
    Start from the basic bound true for every real <m>\theta</m>:
  </p>
  <me>
    -1 \;\le\; \sin(\theta) \;\le\; 1.
  </me>

  <p>
    Substitute <m>\theta = \tfrac{1}{x-1}</m> (valid when <m>x \neq 1</m>):
  </p>
  <me>
    -1 \;\le\; \sin\!\Bigl(\tfrac{1}{x-1}\Bigr) \;\le\; 1.
  </me>

  <p>
    Multiply the entire inequality by <m>4(x-1)^2</m>. Because
    <m>4(x-1)^2 \ge 0</m> for all <m>x</m>, the direction of the inequalities
    is preserved (multiplying by a nonnegative number does not flip signs):
  </p>
  <me>
    -\,4(x-1)^2 \;\le\; 4(x-1)^2 \sin\!\Bigl(\tfrac{1}{x-1}\Bigr) \;\le\; 4(x-1)^2.
  </me>

  <p>
    Now add <m>x</m> to each part of the inequality. Adding the same quantity to
    all three parts preserves the inequalities:
  </p>
  <me>
    x - 4(x-1)^2 \;\le\; x + 4(x-1)^2 \sin\!\Bigl(\tfrac{1}{x-1}\Bigr) \;\le\; x + 4(x-1)^2.
  </me>

  <p>
    Define the “floor” and “ceiling” functions
    <m>g(x)=x-4(x-1)^2</m> and <m>h(x)=x+4(x-1)^2</m>. Compute their limits
    using substitution:
  </p>
  <me>
    \lim_{x\to 1} g(x)
    \;=\; \lim_{x\to 1}\!\Bigl(x - 4(x-1)^2\Bigr)
    \;=\; 1
  </me>
  <me>
    \lim_{x\to 1} h(x)
    \;=\; \lim_{x\to 1}\!\Bigl(x + 4(x-1)^2\Bigr)
    \;=\; 1
  </me>

  <p>
    Since for all <m>x</m> near (but not equal to) <m>1</m> we have
    <m>g(x) \le x + 4(x-1)^2 \sin\!\bigl(\tfrac{1}{x-1}\bigr) \le h(x)</m>,
    and both bounding limits equal <m>1</m>, the Squeeze Theorem gives:
  </p>
  <me>
    \lim_{x\to 1} \Bigl(x + 4(x-1)^2 \sin\!\bigl(\tfrac{1}{x-1}\bigr)\Bigr) \;=\; 1.
  </me>

</solution>
    </example>

<remark>
  <p>
    Trigonometric functions are especially good candidates for the Squeeze Theorem, 
    since they have natural bounds. For example,
  </p>
  <me>
    -1 \leq \sin(\theta) \leq 1
  </me>
  <p>
    and
  </p>
  <me>
    -1 \leq \cos(\theta) \leq 1.
  </me>
  <p>
    These inequalities allow us to evaluate many limits involving 
    trigonometric functions by “squeezing” them between simpler functions.
  </p>
</remark>



    </subsection>

<subsection xml:id="additional-worked-problems-link">
  <title>Additional Worked-Out Examples</title>

  <p>
    To keep this section at a reasonable length, we include only a few representative examples directly in the text.
    If you find yourself stuck on a homework problem, or if you are studying before a quiz or exam, additional
    fully worked-out examples are available via: 
    <alert>
      <xref ref="subsec-limit-alg">
        Additional Examples for Limit Laws and Algebraic Techniques  
      </xref>
    </alert>
  </p>

</subsection>


<exercises xml:id="tf-exercises-limits">
  <title>Practice / Study Problems</title>
  
  <exercisegroup>
    <title>Some True/False Problems</title>
    <introduction>
      <p>
        Determine whether each statement is true or false and explain your reasoning
      </p>
    </introduction>

 <exercise>
    <statement><p>
      If <m>\displaystyle\lim_{x\to 2} f(x) = 5</m> and <m>\displaystyle\lim_{x\to 2} g(x) = 7</m>, then <m>\displaystyle\lim_{x\to 2}(f(x)+g(x))=12</m>.
    </p></statement>
    <solution>
      <p><alert>True.</alert> By the Sum Law: <m>\displaystyle\lim_{x\to 2}(f(x)+g(x))=\displaystyle\lim_{x\to 2} f(x)+\displaystyle\lim_{x\to 2} g(x)=5+7=12</m>.</p>
    </solution>
  </exercise>

  <exercise>
    <statement><p>
      If <m>\displaystyle\lim_{x\to 3} f(x) = 5</m>, then <m>f(3) = 5</m>.
    </p></statement>
    <solution>
      <p><alert>False.</alert> A limit concerns nearby values, not necessarily the value at the point. 
        For example, consider <xref ref="ex-canceling-zeros">Example</xref>. The limit exists even though the function itself is not defined 
        at the point.</p>
    </solution>
  </exercise>

  <exercise>
    <statement><p>
      <m>\displaystyle\lim_{x\to 1}\dfrac{x^2-1}{x-1} = 2</m>.
    </p></statement>
    <solution>
      <p><alert>True.</alert> Factor and cancel: <m>\dfrac{(x-1)(x+1)}{x-1}=x+1</m> for <m>x\neq 1</m>, so <m>\displaystyle\lim_{x\to 1}(x+1)=2</m>.</p>
    </solution>
  </exercise>

  <exercise>
    <statement><p>
      The Limit Laws can always be applied, even if one of the limits does not exist.
    </p></statement>
    <solution>
      <p><alert>False.</alert> Each Limit Law requires the relevant limits to exist. If one does not exist, the law cannot be applied.</p>
    </solution>
  </exercise>

  <exercise>
    <statement><p>
      If <m>\displaystyle\lim_{x\to 2} f(x)=0</m> and <m>\displaystyle\lim_{x\to 2} g(x)=0</m>, then <m>\displaystyle\lim_{x\to 2}\dfrac{f(x)}{g(x)}</m> must equal 1.
    </p></statement>
    <solution>
      <p><alert>False.</alert> The form <m>\tfrac{0}{0}</m> is meaningless. 
        For example, <m>f(x)=x-2</m>, <m>g(x)=2(x-2)</m> give <m>\displaystyle\lim_{x\to 2}\dfrac{f(x)}{g(x)}=\tfrac{1}{2}</m>.</p>
    </solution>
  </exercise>

  <exercise>
    <statement><p>
      If <m>f(x)</m> is a polynomial, then <m>\displaystyle\lim_{x\to a} f(x) = f(a)</m> for every real number <m>a</m>.
    </p></statement>
    <solution>
      <p><alert>True.</alert> Polynomials are continuous. We can apply the limit laws to substitute as discussed previously in this section. So <m>\displaystyle\lim_{x\to a} f(x)=f(a)</m> for all <m>a</m>.</p>
    </solution>
  </exercise>

  <exercise>
    <statement><p>
      <m>\displaystyle\lim_{x\to 0}\dfrac{|x|}{x}</m> does not exist.
    </p></statement>
    <solution>
      <p><alert>True.</alert> <m>\displaystyle\lim_{x\to 0^-}\dfrac{|x|}{x}=-1</m> and <m>\displaystyle\lim_{x\to 0^+}\dfrac{|x|}{x}=1</m>, so the two-sided limit does not exist.</p>
    </solution>
  </exercise>

  <exercise>
    <statement><p>
      <m>\displaystyle\lim_{x\to 1}\dfrac{x^2-4}{x-2} = -3</m>.
    </p></statement>
    <solution>
      <p><alert>False.</alert> Simplify to <m>x+2</m>, so <m>\displaystyle\lim_{x\to 1}(x+2)=3</m>, not <m>-3</m>.</p>
    </solution>
  </exercise>


  <exercise>
    <statement><p>
      <m>\displaystyle\lim_{x\to 0} x\sin(\tfrac{1}{x}) = 0</m>.
    </p></statement>
    <solution>
      <p><alert>True.</alert> By the Squeeze Theorem: <m>-|x|\leq x\sin(\tfrac{1}{x})\leq |x|</m>, and both bounds go to 0 as <m>x\to 0</m>.</p>
    </solution>
  </exercise>

  <exercise>
    <statement><p>
      The Squeeze Theorem can be used to evaluate <m>\displaystyle\lim_{x\to 0} x^2\cos(\tfrac{1}{x})</m>.
    </p></statement>
    <solution>
      <p><alert>True.</alert> Since <m>-1\leq \cos(\tfrac{1}{x})\leq 1</m>, multiplying by <m>x^2\ge 0</m> gives <m>-x^2 \leq x^2\cos(\tfrac{1}{x}) \leq x^2</m>. Both bounds go to 0, so <m>\displaystyle\lim_{x\to 0} x^2\cos(\tfrac{1}{x})=0</m>.</p>
    </solution>
  </exercise>

</exercisegroup>

</exercises>

</section>
